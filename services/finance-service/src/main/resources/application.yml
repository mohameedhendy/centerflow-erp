spring:
  application:
    name: finance-service

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info

  endpoint:
    health:
      show-details: never